diff --git a/node_modules/parse5/dist/common/token.d.ts b/node_modules/parse5/dist/common/token.d.ts
index 9bcc7dc..870ccd0 100644
--- a/node_modules/parse5/dist/common/token.d.ts
+++ b/node_modules/parse5/dist/common/token.d.ts
@@ -11,6 +11,8 @@ export declare enum TokenType {
     HIBERNATION = 8
 }
 export interface Location {
+    /** Actual tag-name input, present only with sourceTextInfo. */
+    sourceName?: SourceTextRange;
     /** One-based line index of the first character. */
     startLine: number;
     /** One-based column index of the first character. */
@@ -49,6 +51,14 @@ export interface DoctypeToken extends TokenBase {
     systemId: string | null;
 }
 export interface Attribute {
+    /** End of the last actual name/value consumption. */
+    sourceEnd?: number;
+    /** Name input before case or namespace normalization. */
+    sourceName?: SourceTextRange;
+    /** Actual decoded-value emissions, present only with sourceTextInfo. */
+    sourceValue?: { from: number; to: number; text: string; range: SourceTextRange }[];
+    /** Unquoted input boundaries, or null when no value was consumed. */
+    sourceValueRange?: { from: number; to: number } | null;
     /** The name of the attribute. */
     name: string;
     /** The namespace of the attribute. */
@@ -59,6 +69,7 @@ export interface Attribute {
     value: string;
 }
 export interface TagToken extends TokenBase {
+    sourceName?: SourceTextRange;
     readonly type: TokenType.START_TAG | TokenType.END_TAG;
     tagName: string;
     /** Used to cache the ID of the tag name. */
@@ -70,13 +81,20 @@ export interface TagToken extends TokenBase {
 }
 export declare function getTokenAttr(token: TagToken, attrName: string): string | null;
 export interface CommentToken extends TokenBase {
+    /** Actual content input, including an explicit boundary for empty data. */
+    sourceDataRange?: { from: number; to: number };
+    /** Actual consumed end; legacy location.endOffset can extend past EOF. */
+    sourceEnd?: number;
+    sourceData?: { from: number; to: number; text: string; range: SourceTextRange }[];
     readonly type: TokenType.COMMENT;
     data: string;
 }
 export interface EOFToken extends TokenBase {
     readonly type: TokenType.EOF;
 }
+export interface SourceTextRange { from: number; to: number; transformed?: boolean; }
 export interface CharacterToken extends TokenBase {
+    source?: {range: SourceTextRange; text: string}[];
     type: TokenType.CHARACTER | TokenType.NULL_CHARACTER | TokenType.WHITESPACE_CHARACTER;
     chars: string;
 }
diff --git a/node_modules/parse5/dist/parser/index.d.ts b/node_modules/parse5/dist/parser/index.d.ts
index 04c6a59..578bcbc 100644
--- a/node_modules/parse5/dist/parser/index.d.ts
+++ b/node_modules/parse5/dist/parser/index.d.ts
@@ -31,6 +31,7 @@ declare enum InsertionMode {
     AFTER_AFTER_FRAMESET = 22
 }
 export interface ParserOptions<T extends TreeAdapterTypeMap> {
+    sourceTextInfo?: boolean;
     /**
      * The [scripting flag](https://html.spec.whatwg.org/multipage/parsing.html#scripting-flag). If set
      * to `true`, `noscript` element content will be parsed as text.
diff --git a/node_modules/parse5/dist/parser/index.js b/node_modules/parse5/dist/parser/index.js
index fdd2562..ff34714 100644
--- a/node_modules/parse5/dist/parser/index.js
+++ b/node_modules/parse5/dist/parser/index.js
@@ -1130,6 +1130,7 @@ export class Parser {
                     return;
                 }
                 token.chars = token.chars.substr(1);
+                if (token.source) token.source = token.source.slice(1);
             }
         }
         if (this.tokenizer.inForeignNode) {
@@ -3184,6 +3185,7 @@ function startTagAfterAfterFrameset(p, token) {
 //------------------------------------------------------------------
 function nullCharacterInForeignContent(p, token) {
     token.chars = unicode.REPLACEMENT_CHARACTER;
+    if (token.source) token.source = [{range:{from:token.source[0].range.from,to:token.source.at(-1).range.to,transformed:true},text:token.chars}];
     p._insertCharacters(token);
 }
 function characterInForeignContent(p, token) {
diff --git a/node_modules/parse5/dist/serializer/index.d.ts b/node_modules/parse5/dist/serializer/index.d.ts
index 995b3d7..0dbd01f 100644
--- a/node_modules/parse5/dist/serializer/index.d.ts
+++ b/node_modules/parse5/dist/serializer/index.d.ts
@@ -1,12 +1,21 @@
 import type { TreeAdapter, TreeAdapterTypeMap } from '../tree-adapters/interface.js';
 import { type DefaultTreeAdapterMap } from '../tree-adapters/default.js';
+export interface SerializerSource<T extends TreeAdapterTypeMap> {
+    node: T['node'];
+    kind: 'syntax' | 'name' | 'text' | 'attribute' | 'comment' | 'doctype';
+    from: number; to: number; output: string;
+    role?: string;
+    attribute?: import('../common/token.js').Attribute;
+    input?: string;
+    replacements?: {from: number; to: number; text: string}[];
+}
 export interface SerializerOptions<T extends TreeAdapterTypeMap> {
     /**
      * Specifies input tree format.
      *
      * @default `treeAdapters.default`
      */
-    treeAdapter?: TreeAdapter<T>;
+    treeAdapter?: Pick<TreeAdapter<T>, 'getTagName' | 'getNamespaceURI' | 'getAttrList' | 'getChildNodes' | 'getParentNode' | 'getTemplateContent' | 'getTextNodeContent' | 'getCommentNodeContent' | 'getDocumentTypeNodeName' | 'isElementNode' | 'isTextNode' | 'isCommentNode' | 'isDocumentTypeNode'>;
     /**
      * The [scripting flag](https://html.spec.whatwg.org/multipage/parsing.html#scripting-flag). If set
      * to `true`, `noscript` element content will not be escaped.
@@ -14,6 +23,10 @@ export interface SerializerOptions<T extends TreeAdapterTypeMap> {
      *  @default `true`
      */
     scriptingEnabled?: boolean;
+    /** Called synchronously for each actual emitted piece, including empty fields. */
+    onSource?: (event: SerializerSource<T>) => void;
+    /** Per-character attribute escaping policy; defaults to HTML serialization. */
+    escapeAttribute?: (character: string) => string;
 }
 /**
  * Serializes an AST node to an HTML string.
diff --git a/node_modules/parse5/dist/serializer/index.js b/node_modules/parse5/dist/serializer/index.js
index 085e2ea..cf259e9 100644
--- a/node_modules/parse5/dist/serializer/index.js
+++ b/node_modules/parse5/dist/serializer/index.js
@@ -1,5 +1,5 @@
 import { TAG_NAMES as $, NS, hasUnescapedText } from '../common/html.js';
-import { escapeText, escapeAttribute } from 'entities/escape';
+import { escapeText, escapeAttribute as escapeAttributeValue } from 'entities/escape';
 import { defaultTreeAdapter } from '../tree-adapters/default.js';
 // Sets
 const VOID_ELEMENTS = new Set([
@@ -27,7 +27,7 @@ function isVoidElement(node, options) {
         options.treeAdapter.getNamespaceURI(node) === NS.HTML &&
         VOID_ELEMENTS.has(options.treeAdapter.getTagName(node)));
 }
-const defaultOpts = { treeAdapter: defaultTreeAdapter, scriptingEnabled: true };
+const defaultOpts = { treeAdapter: defaultTreeAdapter, scriptingEnabled: true, escapeAttribute: escapeAttributeValue };
 /**
  * Serializes an AST node to an HTML string.
  *
@@ -51,7 +51,7 @@ const defaultOpts = { treeAdapter: defaultTreeAdapter, scriptingEnabled: true };
  * @param options Serialization options.
  */
 export function serialize(node, options) {
-    const opts = { ...defaultOpts, ...options };
+    const opts = { ...defaultOpts, ...options, sourceOffset: 0 };
     if (isVoidElement(node, opts)) {
         return '';
     }
@@ -77,7 +77,7 @@ export function serialize(node, options) {
  * @param options Serialization options.
  */
 export function serializeOuter(node, options) {
-    const opts = { ...defaultOpts, ...options };
+    const opts = { ...defaultOpts, ...options, sourceOffset: 0 };
     return serializeNode(node, opts);
 }
 function serializeChildNodes(parentNode, options) {
@@ -112,57 +112,74 @@ function serializeNode(node, options) {
     // Return an empty string for unknown nodes
     return '';
 }
+// Each record is emitted while writing its actual output; no output search.
+function emit(options, node, kind, output, extra = {}) {
+    const from = options.sourceOffset;
+    options.sourceOffset += output.length;
+    options.onSource?.({node, kind, from, to: options.sourceOffset, output, ...extra});
+    return output;
+}
+function escaped(input, pattern, escape) {
+    const replacements = [];
+    let delta = 0;
+    const output = input.replace(pattern, (raw, offset) => {
+        const text = escape(raw);
+        if (text !== raw) replacements.push({from: offset + delta, to: offset + delta + raw.length, text});
+        delta += text.length - raw.length;
+        return text;
+    });
+    return {input, output, replacements};
+}
 function serializeElement(node, options) {
     const tn = options.treeAdapter.getTagName(node);
-    return `<${tn}${serializeAttributes(node, options)}>${isVoidElement(node, options) ? '' : `${serializeChildNodes(node, options)}</${tn}>`}`;
+    let html = emit(options, node, 'syntax', '<', {role: 'start-open'});
+    html += emit(options, node, 'name', tn, {role: 'start'});
+    html += serializeAttributes(node, options);
+    html += emit(options, node, 'syntax', '>', {role: 'start-close'});
+    if (!isVoidElement(node, options)) {
+        html += serializeChildNodes(node, options);
+        html += emit(options, node, 'syntax', '</', {role: 'end-open'});
+        html += emit(options, node, 'name', tn, {role: 'end'});
+        html += emit(options, node, 'syntax', '>', {role: 'end-close'});
+    }
+    return html;
 }
-function serializeAttributes(node, { treeAdapter }) {
+function serializeAttributes(node, options) {
     let html = '';
-    for (const attr of treeAdapter.getAttrList(node)) {
-        html += ' ';
+    for (const attr of options.treeAdapter.getAttrList(node)) {
+        html += emit(options, node, 'syntax', ' ', {role: 'attribute-space', attribute: attr});
+        let name;
         if (attr.namespace) {
             switch (attr.namespace) {
-                case NS.XML: {
-                    html += `xml:${attr.name}`;
-                    break;
-                }
-                case NS.XMLNS: {
-                    if (attr.name !== 'xmlns') {
-                        html += 'xmlns:';
-                    }
-                    html += attr.name;
-                    break;
-                }
-                case NS.XLINK: {
-                    html += `xlink:${attr.name}`;
-                    break;
-                }
-                default: {
-                    html += `${attr.prefix}:${attr.name}`;
-                }
+                case NS.XML: name = `xml:${attr.name}`; break;
+                case NS.XMLNS: name = attr.name === 'xmlns' ? attr.name : `xmlns:${attr.name}`; break;
+                case NS.XLINK: name = `xlink:${attr.name}`; break;
+                default: name = `${attr.prefix}:${attr.name}`;
             }
-        }
-        else {
-            html += attr.name;
-        }
-        html += `="${escapeAttribute(attr.value)}"`;
+        } else name = attr.name;
+        html += emit(options, node, 'name', name, {role: 'attribute', attribute: attr});
+        html += emit(options, node, 'syntax', '="', {role: 'attribute-value-open', attribute: attr});
+        const value = escaped(attr.value, /["&<>\u00a0]/g, options.escapeAttribute);
+        html += emit(options, node, 'attribute', value.output, {...value, attribute: attr});
+        html += emit(options, node, 'syntax', '"', {role: 'attribute-value-close', attribute: attr});
     }
     return html;
 }
 function serializeTextNode(node, options) {
-    const { treeAdapter } = options;
-    const content = treeAdapter.getTextNodeContent(node);
+    const {treeAdapter} = options;
+    const input = treeAdapter.getTextNodeContent(node);
     const parent = treeAdapter.getParentNode(node);
     const parentTn = parent && treeAdapter.isElementNode(parent) && treeAdapter.getTagName(parent);
-    return parentTn &&
-        treeAdapter.getNamespaceURI(parent) === NS.HTML &&
-        hasUnescapedText(parentTn, options.scriptingEnabled)
-        ? content
-        : escapeText(content);
+    const unescaped = parentTn && treeAdapter.getNamespaceURI(parent) === NS.HTML && hasUnescapedText(parentTn, options.scriptingEnabled);
+    const value = unescaped ? {input, output: input, replacements: []} : escaped(input, /[&<>\u00a0]/g, escapeText);
+    return emit(options, node, 'text', value.output, value);
 }
-function serializeCommentNode(node, { treeAdapter }) {
-    return `<!--${treeAdapter.getCommentNodeContent(node)}-->`;
+function serializeCommentNode(node, options) {
+    let html = emit(options, node, 'syntax', '<!--', {role: 'comment-open'});
+    const input = options.treeAdapter.getCommentNodeContent(node);
+    html += emit(options, node, 'comment', input, {input, replacements: []});
+    return html + emit(options, node, 'syntax', '-->', {role: 'comment-close'});
 }
-function serializeDocumentTypeNode(node, { treeAdapter }) {
-    return `<!DOCTYPE ${treeAdapter.getDocumentTypeNodeName(node)}>`;
+function serializeDocumentTypeNode(node, options) {
+    return emit(options, node, 'doctype', `<!DOCTYPE ${options.treeAdapter.getDocumentTypeNodeName(node)}>`);
 }
diff --git a/node_modules/parse5/dist/tokenizer/index.d.ts b/node_modules/parse5/dist/tokenizer/index.d.ts
index 3de1750..452b92e 100644
--- a/node_modules/parse5/dist/tokenizer/index.d.ts
+++ b/node_modules/parse5/dist/tokenizer/index.d.ts
@@ -86,6 +86,7 @@ export declare const TokenizerMode: {
     readonly CDATA_SECTION: State.CDATA_SECTION;
 };
 export interface TokenizerOptions {
+    sourceTextInfo?: boolean;
     sourceCodeLocationInfo?: boolean;
 }
 export interface TokenHandler {
@@ -150,10 +151,18 @@ export declare class Tokenizer {
     protected _consumeSequenceIfMatch(pattern: string, caseSensitive: boolean): boolean;
     protected _createStartTagToken(): void;
     protected _createEndTagToken(): void;
-    protected _createCommentToken(offset: number): void;
+    protected _createCommentToken(offset: number, contentFrom?: number): void;
+    protected _appendCommentData(text: string, range?: import('../common/token.js').SourceTextRange, transformed?: boolean): void;
+    protected _appendPendingCommentData(text: string, distance?: number): void;
     protected _createDoctypeToken(initialName: string | null): void;
     protected _createCharacterToken(type: CharacterToken['type'], chars: string): void;
     protected _createAttr(attrNameFirstCh: string): void;
+    protected _appendAttrValue(text: string, range?: import('../common/token.js').SourceTextRange, transformed?: boolean): void;
+    protected _beginAttrValue(quoted: boolean): void;
+    protected _endAttrValue(quoted?: boolean): void;
+    protected _appendAttrName(text: string): void;
+    protected _appendTagName(text: string): void;
+    protected _recordTagName(range: import('../common/token.js').SourceTextRange): void;
     protected _leaveAttrName(): void;
     protected _leaveAttrValue(): void;
     protected prepareToken(ct: Token): void;
@@ -162,9 +171,9 @@ export declare class Tokenizer {
     protected emitCurrentDoctype(ct: DoctypeToken): void;
     protected _emitCurrentCharacterToken(nextLocation: Location | null): void;
     protected _emitEOFToken(): void;
-    protected _appendCharToCurrentCharacterToken(type: CharacterToken['type'], ch: string): void;
+    protected _appendCharToCurrentCharacterToken(type: CharacterToken['type'], ch: string, range?: import('../common/token.js').SourceTextRange): void;
     protected _emitCodePoint(cp: number): void;
-    protected _emitChars(ch: string): void;
+    protected _emitChars(ch: string, from?: number, to?: number, transformed?: boolean): void;
     protected _startCharacterReference(): void;
     protected _isCharacterReferenceInAttribute(): boolean;
     protected _flushCodePointConsumedAsCharacterReference(cp: number): void;
diff --git a/node_modules/parse5/dist/tokenizer/index.js b/node_modules/parse5/dist/tokenizer/index.js
index 95f7846..5ed7275 100644
--- a/node_modules/parse5/dist/tokenizer/index.js
+++ b/node_modules/parse5/dist/tokenizer/index.js
@@ -297,13 +297,26 @@ export class Tokenizer {
             location: this.getCurrentLocation(2),
         };
     }
-    _createCommentToken(offset) {
+    _createCommentToken(offset, contentFrom = this.preprocessor.sourceRange?.from) {
         this.currentToken = {
             type: TokenType.COMMENT,
             data: '',
+            ...(this.options.sourceTextInfo ? {sourceData: [], sourceDataRange: {from:contentFrom,to:contentFrom}} : {}),
             location: this.getCurrentLocation(offset),
         };
     }
+    _appendCommentData(text, range = this.preprocessor.sourceRange, transformed = false) {
+        const token = this.currentToken, from = token.data.length;
+        token.data += text;
+        if (this.options.sourceTextInfo) {
+            token.sourceData.push({from,to:from+text.length,text,range:transformed?{...range,transformed:true}:range});
+            token.sourceDataRange.to = range.to;
+        }
+    }
+    _appendPendingCommentData(text, distance = text.length) {
+        const end = this.preprocessor.sourceRange?.from;
+        this._appendCommentData(text, {from:end-distance,to:end-distance+text.length});
+    }
     _createDoctypeToken(initialName) {
         this.currentToken = {
             type: TokenType.DOCTYPE,
@@ -326,10 +339,37 @@ export class Tokenizer {
         this.currentAttr = {
             name: attrNameFirstCh,
             value: '',
+            ...(this.options.sourceTextInfo ? {sourceValue: [], sourceValueRange: null, sourceName: {from:this.preprocessor.sourceRange.from,to:attrNameFirstCh ? this.preprocessor.sourceRange.to : this.preprocessor.sourceRange.from}} : {}),
         };
         this.currentLocation = this.getCurrentLocation(0);
     }
+    _appendAttrValue(text, range = this.preprocessor.sourceRange, transformed = false) {
+        const from = this.currentAttr.value.length;
+        this.currentAttr.value += text;
+        if (this.options.sourceTextInfo) {
+            this.currentAttr.sourceValue.push({
+                from, to: from + text.length, text,
+                range: transformed ? { ...range, transformed: true } : range,
+            });
+        }
+    }
+    _appendAttrName(text) {
+        this.currentAttr.name += text;
+        if (this.options.sourceTextInfo) this.currentAttr.sourceName.to = this.preprocessor.sourceRange.to;
+    }
+    _appendTagName(text) {
+        this.currentToken.tagName += text;
+        this._recordTagName(this.preprocessor.sourceRange);
+    }
+    _recordTagName(range) {
+        if (!this.options.sourceTextInfo) return;
+        const token = this.currentToken;
+        if (token.sourceName) token.sourceName.to = range.to;
+        else token.sourceName = {from:range.from,to:range.to};
+        if (token.location) token.location.sourceName = token.sourceName;
+    }
     _leaveAttrName() {
+        if (this.options.sourceTextInfo) this.currentAttr.sourceEnd = this.preprocessor.sourceRange.from;
         var _a;
         var _b;
         const token = this.currentToken;
@@ -346,7 +386,20 @@ export class Tokenizer {
             this._err(ERR.duplicateAttribute);
         }
     }
+    _beginAttrValue(quoted) {
+        if (this.options.sourceTextInfo) {
+            const from = quoted ? this.preprocessor.sourceRange.to : this.preprocessor.sourceRange.from;
+            this.currentAttr.sourceValueRange = { from, to: from };
+        }
+    }
+    _endAttrValue(quoted = false) {
+        if (this.options.sourceTextInfo) this.currentAttr.sourceEnd = quoted ? this.preprocessor.sourceRange.to : this.preprocessor.sourceRange.from;
+        if (this.currentAttr.sourceValueRange) {
+            this.currentAttr.sourceValueRange.to = this.preprocessor.sourceRange.from;
+        }
+    }
     _leaveAttrValue() {
+        if (this.options.sourceTextInfo) this.currentAttr.sourceEnd = this.preprocessor.offset;
         if (this.currentLocation) {
             this.currentLocation.endLine = this.preprocessor.line;
             this.currentLocation.endCol = this.preprocessor.col;
@@ -384,6 +437,7 @@ export class Tokenizer {
         this.preprocessor.dropParsedChunk();
     }
     emitCurrentComment(ct) {
+        if (this.options.sourceTextInfo) ct.sourceEnd = this.preprocessor.sourceRange.to;
         this.prepareToken(ct);
         this.handler.onComment(ct);
         this.preprocessor.dropParsedChunk();
@@ -439,10 +493,11 @@ export class Tokenizer {
     //1)TokenType.NULL_CHARACTER - \u0000-character sequences (e.g. '\u0000\u0000\u0000')
     //2)TokenType.WHITESPACE_CHARACTER - any whitespace/new-line character sequences (e.g. '\n  \r\t   \f')
     //3)TokenType.CHARACTER - any character sequence which don't belong to groups 1 and 2 (e.g. 'abcdef1234@@#$%^')
-    _appendCharToCurrentCharacterToken(type, ch) {
+    _appendCharToCurrentCharacterToken(type, ch, range) {
         if (this.currentCharacterToken) {
             if (this.currentCharacterToken.type === type) {
                 this.currentCharacterToken.chars += ch;
+                if (range) this.currentCharacterToken.source.push({range,text:ch});
                 return;
             }
             else {
@@ -452,6 +507,7 @@ export class Tokenizer {
             }
         }
         this._createCharacterToken(type, ch);
+        if (range) this.currentCharacterToken.source = [{range,text:ch}];
     }
     _emitCodePoint(cp) {
         const type = isWhitespace(cp)
@@ -461,12 +517,12 @@ export class Tokenizer {
                 : TokenType.CHARACTER;
         // OPTIMIZATION: Use String.fromCharCode for BMP characters (< 0x10000) which is faster
         // than String.fromCodePoint. Characters outside BMP are rare in HTML.
-        this._appendCharToCurrentCharacterToken(type, cp < 65536 ? String.fromCharCode(cp) : String.fromCodePoint(cp));
+        this._appendCharToCurrentCharacterToken(type, cp < 65536 ? String.fromCharCode(cp) : String.fromCodePoint(cp), this.options.sourceTextInfo ? this.sourceEntityRange ?? this.preprocessor.sourceRange : undefined);
     }
     //NOTE: used when we emit characters explicitly.
     //This is always for non-whitespace and non-null characters, which allows us to avoid additional checks.
-    _emitChars(ch) {
-        this._appendCharToCurrentCharacterToken(TokenType.CHARACTER, ch);
+    _emitChars(ch, from, to, transformed = false) {
+        this._appendCharToCurrentCharacterToken(TokenType.CHARACTER, ch, this.options.sourceTextInfo ? (from === undefined ? (transformed ? {...this.preprocessor.sourceRange,transformed:true} : this.preprocessor.sourceRange) : {from,to}) : undefined);
     }
     // Character reference helpers
     _startCharacterReference() {
@@ -482,10 +538,16 @@ export class Tokenizer {
     }
     _flushCodePointConsumedAsCharacterReference(cp) {
         if (this._isCharacterReferenceInAttribute()) {
-            this.currentAttr.value += String.fromCodePoint(cp);
+            this._appendAttrValue(String.fromCodePoint(cp), {
+                from: this.preprocessor.droppedBufferSize + this.entityStartPos,
+                to: this.preprocessor.offset + 1,
+                transformed: true,
+            });
         }
         else {
-            this._emitCodePoint(cp);
+            const previous = this.sourceEntityRange;
+            this.sourceEntityRange = {from:this.preprocessor.droppedBufferSize + this.entityStartPos,to:this.preprocessor.offset+1,transformed:true};
+            try { this._emitCodePoint(cp); } finally { this.sourceEntityRange = previous; }
         }
     }
     // Calling states this way turns out to be much faster than any other approach.
@@ -829,7 +891,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -851,7 +913,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -873,7 +935,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -891,7 +953,7 @@ export class Tokenizer {
         switch (cp) {
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -930,13 +992,13 @@ export class Tokenizer {
                 }
                 case $.EOF: {
                     this._err(ERR.eofBeforeTagName);
-                    this._emitChars('<');
+                    this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
                     this._emitEOFToken();
                     break;
                 }
                 default: {
                     this._err(ERR.invalidFirstCharacterOfTagName);
-                    this._emitChars('<');
+                    this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
                     this.state = State.DATA;
                     this._stateData(cp);
                 }
@@ -959,7 +1021,7 @@ export class Tokenizer {
                 }
                 case $.EOF: {
                     this._err(ERR.eofBeforeTagName);
-                    this._emitChars('</');
+                    this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
                     this._emitEOFToken();
                     break;
                 }
@@ -994,7 +1056,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                token.tagName += REPLACEMENT_CHARACTER;
+                this._appendTagName(REPLACEMENT_CHARACTER);
                 break;
             }
             case $.EOF: {
@@ -1003,7 +1065,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.tagName += String.fromCodePoint(isAsciiUpper(cp) ? toAsciiLower(cp) : cp);
+                this._appendTagName(String.fromCodePoint(isAsciiUpper(cp) ? toAsciiLower(cp) : cp));
             }
         }
     }
@@ -1014,7 +1076,7 @@ export class Tokenizer {
             this.state = State.RCDATA_END_TAG_OPEN;
         }
         else {
-            this._emitChars('<');
+            this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RCDATA;
             this._stateRcdata(cp);
         }
@@ -1027,7 +1089,7 @@ export class Tokenizer {
             this._stateRcdataEndTagName(cp);
         }
         else {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RCDATA;
             this._stateRcdata(cp);
         }
@@ -1039,6 +1101,7 @@ export class Tokenizer {
         this._createEndTagToken();
         const token = this.currentToken;
         token.tagName = this.lastStartTagName;
+        const nameFrom = this.preprocessor.sourceRange?.from;
         const cp = this.preprocessor.peek(this.lastStartTagName.length);
         switch (cp) {
             case $.SPACE:
@@ -1046,16 +1109,19 @@ export class Tokenizer {
             case $.TABULATION:
             case $.FORM_FEED: {
                 this._advanceBy(this.lastStartTagName.length);
+                this._recordTagName({from:nameFrom,to:nameFrom+this.lastStartTagName.length});
                 this.state = State.BEFORE_ATTRIBUTE_NAME;
                 return false;
             }
             case $.SOLIDUS: {
                 this._advanceBy(this.lastStartTagName.length);
+                this._recordTagName({from:nameFrom,to:nameFrom+this.lastStartTagName.length});
                 this.state = State.SELF_CLOSING_START_TAG;
                 return false;
             }
             case $.GREATER_THAN_SIGN: {
                 this._advanceBy(this.lastStartTagName.length);
+                this._recordTagName({from:nameFrom,to:nameFrom+this.lastStartTagName.length});
                 this.emitCurrentTagToken();
                 this.state = State.DATA;
                 return false;
@@ -1069,7 +1135,7 @@ export class Tokenizer {
     //------------------------------------------------------------------
     _stateRcdataEndTagName(cp) {
         if (this.handleSpecialEndTag(cp)) {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RCDATA;
             this._stateRcdata(cp);
         }
@@ -1081,7 +1147,7 @@ export class Tokenizer {
             this.state = State.RAWTEXT_END_TAG_OPEN;
         }
         else {
-            this._emitChars('<');
+            this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RAWTEXT;
             this._stateRawtext(cp);
         }
@@ -1094,7 +1160,7 @@ export class Tokenizer {
             this._stateRawtextEndTagName(cp);
         }
         else {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RAWTEXT;
             this._stateRawtext(cp);
         }
@@ -1103,7 +1169,7 @@ export class Tokenizer {
     //------------------------------------------------------------------
     _stateRawtextEndTagName(cp) {
         if (this.handleSpecialEndTag(cp)) {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.RAWTEXT;
             this._stateRawtext(cp);
         }
@@ -1118,11 +1184,11 @@ export class Tokenizer {
             }
             case $.EXCLAMATION_MARK: {
                 this.state = State.SCRIPT_DATA_ESCAPE_START;
-                this._emitChars('<!');
+                this._emitChars('<!', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)+1);
                 break;
             }
             default: {
-                this._emitChars('<');
+                this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
                 this.state = State.SCRIPT_DATA;
                 this._stateScriptData(cp);
             }
@@ -1136,7 +1202,7 @@ export class Tokenizer {
             this._stateScriptDataEndTagName(cp);
         }
         else {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA;
             this._stateScriptData(cp);
         }
@@ -1145,7 +1211,7 @@ export class Tokenizer {
     //------------------------------------------------------------------
     _stateScriptDataEndTagName(cp) {
         if (this.handleSpecialEndTag(cp)) {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA;
             this._stateScriptData(cp);
         }
@@ -1189,7 +1255,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1218,7 +1284,7 @@ export class Tokenizer {
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
                 this.state = State.SCRIPT_DATA_ESCAPED;
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1252,7 +1318,7 @@ export class Tokenizer {
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
                 this.state = State.SCRIPT_DATA_ESCAPED;
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1273,12 +1339,12 @@ export class Tokenizer {
             this.state = State.SCRIPT_DATA_ESCAPED_END_TAG_OPEN;
         }
         else if (isAsciiLetter(cp)) {
-            this._emitChars('<');
+            this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA_DOUBLE_ESCAPE_START;
             this._stateScriptDataDoubleEscapeStart(cp);
         }
         else {
-            this._emitChars('<');
+            this._emitChars('<', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA_ESCAPED;
             this._stateScriptDataEscaped(cp);
         }
@@ -1291,7 +1357,7 @@ export class Tokenizer {
             this._stateScriptDataEscapedEndTagName(cp);
         }
         else {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA_ESCAPED;
             this._stateScriptDataEscaped(cp);
         }
@@ -1300,7 +1366,7 @@ export class Tokenizer {
     //------------------------------------------------------------------
     _stateScriptDataEscapedEndTagName(cp) {
         if (this.handleSpecialEndTag(cp)) {
-            this._emitChars('</');
+            this._emitChars('</', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
             this.state = State.SCRIPT_DATA_ESCAPED;
             this._stateScriptDataEscaped(cp);
         }
@@ -1337,7 +1403,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1367,7 +1433,7 @@ export class Tokenizer {
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
                 this.state = State.SCRIPT_DATA_DOUBLE_ESCAPED;
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1402,7 +1468,7 @@ export class Tokenizer {
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
                 this.state = State.SCRIPT_DATA_DOUBLE_ESCAPED;
-                this._emitChars(REPLACEMENT_CHARACTER);
+                this._emitChars(REPLACEMENT_CHARACTER, undefined, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1500,16 +1566,16 @@ export class Tokenizer {
             case $.APOSTROPHE:
             case $.LESS_THAN_SIGN: {
                 this._err(ERR.unexpectedCharacterInAttributeName);
-                this.currentAttr.name += String.fromCodePoint(cp);
+                this._appendAttrName(String.fromCodePoint(cp));
                 break;
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this.currentAttr.name += REPLACEMENT_CHARACTER;
+                this._appendAttrName(REPLACEMENT_CHARACTER);
                 break;
             }
             default: {
-                this.currentAttr.name += String.fromCodePoint(isAsciiUpper(cp) ? toAsciiLower(cp) : cp);
+                this._appendAttrName(String.fromCodePoint(isAsciiUpper(cp) ? toAsciiLower(cp) : cp));
             }
         }
     }
@@ -1561,20 +1627,24 @@ export class Tokenizer {
                 break;
             }
             case $.QUOTATION_MARK: {
+                this._beginAttrValue(true);
                 this.state = State.ATTRIBUTE_VALUE_DOUBLE_QUOTED;
                 break;
             }
             case $.APOSTROPHE: {
+                this._beginAttrValue(true);
                 this.state = State.ATTRIBUTE_VALUE_SINGLE_QUOTED;
                 break;
             }
             case $.GREATER_THAN_SIGN: {
                 this._err(ERR.missingAttributeValue);
+                if (this.options.sourceTextInfo) this.currentAttr.sourceEnd = this.preprocessor.sourceRange.from;
                 this.state = State.DATA;
                 this.emitCurrentTagToken();
                 break;
             }
             default: {
+                this._beginAttrValue(false);
                 this.state = State.ATTRIBUTE_VALUE_UNQUOTED;
                 this._stateAttributeValueUnquoted(cp);
             }
@@ -1585,6 +1655,7 @@ export class Tokenizer {
     _stateAttributeValueDoubleQuoted(cp) {
         switch (cp) {
             case $.QUOTATION_MARK: {
+                this._endAttrValue(true);
                 this.state = State.AFTER_ATTRIBUTE_VALUE_QUOTED;
                 break;
             }
@@ -1594,7 +1665,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this.currentAttr.value += REPLACEMENT_CHARACTER;
+                this._appendAttrValue(REPLACEMENT_CHARACTER, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1603,7 +1674,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                this.currentAttr.value += String.fromCodePoint(cp);
+                this._appendAttrValue(String.fromCodePoint(cp));
             }
         }
     }
@@ -1612,6 +1683,7 @@ export class Tokenizer {
     _stateAttributeValueSingleQuoted(cp) {
         switch (cp) {
             case $.APOSTROPHE: {
+                this._endAttrValue(true);
                 this.state = State.AFTER_ATTRIBUTE_VALUE_QUOTED;
                 break;
             }
@@ -1621,7 +1693,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this.currentAttr.value += REPLACEMENT_CHARACTER;
+                this._appendAttrValue(REPLACEMENT_CHARACTER, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1630,7 +1702,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                this.currentAttr.value += String.fromCodePoint(cp);
+                this._appendAttrValue(String.fromCodePoint(cp));
             }
         }
     }
@@ -1642,6 +1714,7 @@ export class Tokenizer {
             case $.LINE_FEED:
             case $.TABULATION:
             case $.FORM_FEED: {
+                this._endAttrValue();
                 this._leaveAttrValue();
                 this.state = State.BEFORE_ATTRIBUTE_NAME;
                 break;
@@ -1651,6 +1724,7 @@ export class Tokenizer {
                 break;
             }
             case $.GREATER_THAN_SIGN: {
+                this._endAttrValue();
                 this._leaveAttrValue();
                 this.state = State.DATA;
                 this.emitCurrentTagToken();
@@ -1658,7 +1732,7 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                this.currentAttr.value += REPLACEMENT_CHARACTER;
+                this._appendAttrValue(REPLACEMENT_CHARACTER, undefined, true);
                 break;
             }
             case $.QUOTATION_MARK:
@@ -1667,7 +1741,7 @@ export class Tokenizer {
             case $.EQUALS_SIGN:
             case $.GRAVE_ACCENT: {
                 this._err(ERR.unexpectedCharacterInUnquotedAttributeValue);
-                this.currentAttr.value += String.fromCodePoint(cp);
+                this._appendAttrValue(String.fromCodePoint(cp));
                 break;
             }
             case $.EOF: {
@@ -1676,7 +1750,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                this.currentAttr.value += String.fromCodePoint(cp);
+                this._appendAttrValue(String.fromCodePoint(cp));
             }
         }
     }
@@ -1755,11 +1829,11 @@ export class Tokenizer {
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                token.data += REPLACEMENT_CHARACTER;
+                this._appendCommentData(REPLACEMENT_CHARACTER, undefined, true);
                 break;
             }
             default: {
-                token.data += String.fromCodePoint(cp);
+                this._appendCommentData(String.fromCodePoint(cp));
             }
         }
     }
@@ -1767,7 +1841,7 @@ export class Tokenizer {
     //------------------------------------------------------------------
     _stateMarkupDeclarationOpen(cp) {
         if (this._consumeSequenceIfMatch($$.DASH_DASH, true)) {
-            this._createCommentToken($$.DASH_DASH.length + 1);
+            this._createCommentToken($$.DASH_DASH.length + 1, this.preprocessor.sourceRange?.to);
             this.state = State.COMMENT_START;
         }
         else if (this._consumeSequenceIfMatch($$.DOCTYPE, false)) {
@@ -1781,8 +1855,8 @@ export class Tokenizer {
             }
             else {
                 this._err(ERR.cdataInHtmlContent);
-                this._createCommentToken($$.CDATA_START.length + 1);
-                this.currentToken.data = '[CDATA[';
+                this._createCommentToken($$.CDATA_START.length + 1, this.preprocessor.sourceRange?.to - $$.CDATA_START.length);
+                this._appendCommentData('[CDATA[', {from:this.preprocessor.sourceRange.to-7,to:this.preprocessor.sourceRange.to});
                 this.state = State.BOGUS_COMMENT;
             }
         }
@@ -1838,7 +1912,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.data += '-';
+                this._appendPendingCommentData('-');
                 this.state = State.COMMENT;
                 this._stateComment(cp);
             }
@@ -1854,13 +1928,13 @@ export class Tokenizer {
                 break;
             }
             case $.LESS_THAN_SIGN: {
-                token.data += '<';
+                this._appendCommentData('<');
                 this.state = State.COMMENT_LESS_THAN_SIGN;
                 break;
             }
             case $.NULL: {
                 this._err(ERR.unexpectedNullCharacter);
-                token.data += REPLACEMENT_CHARACTER;
+                this._appendCommentData(REPLACEMENT_CHARACTER, undefined, true);
                 break;
             }
             case $.EOF: {
@@ -1870,7 +1944,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.data += String.fromCodePoint(cp);
+                this._appendCommentData(String.fromCodePoint(cp));
             }
         }
     }
@@ -1880,12 +1954,12 @@ export class Tokenizer {
         const token = this.currentToken;
         switch (cp) {
             case $.EXCLAMATION_MARK: {
-                token.data += '!';
+                this._appendCommentData('!');
                 this.state = State.COMMENT_LESS_THAN_SIGN_BANG;
                 break;
             }
             case $.LESS_THAN_SIGN: {
-                token.data += '<';
+                this._appendCommentData('<');
                 break;
             }
             default: {
@@ -1941,7 +2015,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.data += '-';
+                this._appendPendingCommentData('-');
                 this.state = State.COMMENT;
                 this._stateComment(cp);
             }
@@ -1962,7 +2036,7 @@ export class Tokenizer {
                 break;
             }
             case $.HYPHEN_MINUS: {
-                token.data += '-';
+                this._appendPendingCommentData('-', 2);
                 break;
             }
             case $.EOF: {
@@ -1972,7 +2046,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.data += '--';
+                this._appendPendingCommentData('--');
                 this.state = State.COMMENT;
                 this._stateComment(cp);
             }
@@ -1984,7 +2058,7 @@ export class Tokenizer {
         const token = this.currentToken;
         switch (cp) {
             case $.HYPHEN_MINUS: {
-                token.data += '--!';
+                this._appendPendingCommentData('--!');
                 this.state = State.COMMENT_END_DASH;
                 break;
             }
@@ -2001,7 +2075,7 @@ export class Tokenizer {
                 break;
             }
             default: {
-                token.data += '--!';
+                this._appendPendingCommentData('--!');
                 this.state = State.COMMENT;
                 this._stateComment(cp);
             }
@@ -2638,7 +2712,7 @@ export class Tokenizer {
             this.state = State.CDATA_SECTION_END;
         }
         else {
-            this._emitChars(']');
+            this._emitChars(']', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-1+1);
             this.state = State.CDATA_SECTION;
             this._stateCdataSection(cp);
         }
@@ -2652,11 +2726,11 @@ export class Tokenizer {
                 break;
             }
             case $.RIGHT_SQUARE_BRACKET: {
-                this._emitChars(']');
+                this._emitChars(']', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2+1);
                 break;
             }
             default: {
-                this._emitChars(']]');
+                this._emitChars(']]', (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset)-2, (this.preprocessor.sourceRange?.from ?? this.preprocessor.offset));
                 this.state = State.CDATA_SECTION;
                 this._stateCdataSection(cp);
             }
diff --git a/node_modules/parse5/dist/tokenizer/preprocessor.d.ts b/node_modules/parse5/dist/tokenizer/preprocessor.d.ts
index 1dfede3..edc47ff 100644
--- a/node_modules/parse5/dist/tokenizer/preprocessor.d.ts
+++ b/node_modules/parse5/dist/tokenizer/preprocessor.d.ts
@@ -1,5 +1,7 @@
 import { ERR, type ParserError, type ParserErrorHandler } from '../common/error-codes.js';
 export declare class Preprocessor {
+    sourceRange?: import('../common/token.js').SourceTextRange;
+    sourceCarriageReturn?: import('../common/token.js').SourceTextRange;
     private handler;
     html: string;
     pos: number;
diff --git a/node_modules/parse5/dist/tokenizer/preprocessor.js b/node_modules/parse5/dist/tokenizer/preprocessor.js
index 530407b..524e80f 100644
--- a/node_modules/parse5/dist/tokenizer/preprocessor.js
+++ b/node_modules/parse5/dist/tokenizer/preprocessor.js
@@ -139,13 +139,17 @@ export class Preprocessor {
         }
         if (this.pos >= this.html.length) {
             this.endOfChunkHit = !this.lastChunkWritten;
+            if (this.handler.options?.sourceTextInfo) this.sourceRange = {from:this.offset,to:this.offset};
             return $.EOF;
         }
         let cp = this.html.charCodeAt(this.pos);
+        if (this.handler.options?.sourceTextInfo) this.sourceRange = {from:this.offset,to:this.offset+1};
         //NOTE: all U+000D CARRIAGE RETURN (CR) characters must be converted to U+000A LINE FEED (LF) characters
         if (cp === $.CARRIAGE_RETURN) {
             this.isEol = true;
             this.skipNextNewLine = true;
+            this.sourceCarriageReturn = this.sourceRange;
+            if (this.sourceRange) this.sourceRange.transformed = true;
             return $.LINE_FEED;
         }
         //NOTE: any U+000A LINE FEED (LF) characters that immediately follow a U+000D CARRIAGE RETURN (CR) character
@@ -155,6 +159,7 @@ export class Preprocessor {
             if (this.skipNextNewLine) {
                 // `line` will be bumped again in the recursive call.
                 this.line--;
+                if (this.sourceCarriageReturn) this.sourceCarriageReturn.to = this.offset + 1;
                 this.skipNextNewLine = false;
                 this._addGap();
                 return this.advance();
@@ -163,6 +168,7 @@ export class Preprocessor {
         this.skipNextNewLine = false;
         if (isSurrogate(cp)) {
             cp = this._processSurrogate(cp);
+            if (this.sourceRange) this.sourceRange.to = this.offset + 1;
         }
         //OPTIMIZATION: first check if code point is in the common allowed
         //range (ASCII alphanumeric, whitespaces, big chunk of BMP)
