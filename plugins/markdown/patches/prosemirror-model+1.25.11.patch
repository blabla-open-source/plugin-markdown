diff --git a/node_modules/prosemirror-model/dist/index.cjs b/node_modules/prosemirror-model/dist/index.cjs
index 96ef508..e1b1a0f 100644
--- a/node_modules/prosemirror-model/dist/index.cjs
+++ b/node_modules/prosemirror-model/dist/index.cjs
@@ -2211,8 +2211,19 @@ function wsOptionsFor(type, preserveWhitespace, base) {
   if (preserveWhitespace != null) return (preserveWhitespace ? OPT_PRESERVE_WS : 0) | (preserveWhitespace === "full" ? OPT_PRESERVE_WS_FULL : 0);
   return type && type.whitespace == "pre" ? OPT_PRESERVE_WS | OPT_PRESERVE_WS_FULL : base & ~OPT_OPEN_LEFT;
 }
+function replaceSourceText(value, pattern, replacement, source) {
+  if (!source) return value.replace(pattern, replacement)
+  let delta = 0
+  return value.replace(pattern, (raw, offset) => {
+    if (raw !== replacement) source.replacements.push({from: offset + delta, to: offset + delta + raw.length, text: replacement})
+    delta += replacement.length - raw.length
+    return replacement
+  })
+}
+
 var NodeContext = function () {
-  function NodeContext(type, attrs, marks, solid, match, options) {
+  function NodeContext(type, attrs, marks, solid, match, options, onSource) {
+    this.onSource = onSource;
     _classCallCheck(this, NodeContext);
     this.type = type;
     this.attrs = attrs;
@@ -2252,12 +2263,18 @@ var NodeContext = function () {
           m;
         if (last && last.isText && (m = /[ \t\r\n\u000c]+$/.exec(last.text))) {
           var text = last;
-          if (last.text.length == m[0].length) this.content.pop();else this.content[this.content.length - 1] = text.withText(text.text.slice(0, text.text.length - m[0].length));
+          if (last.text.length == m[0].length) this.content.pop();else {
+            var trimmed = text.withText(text.text.slice(0, text.text.length - m[0].length));
+            this.content[this.content.length - 1] = trimmed;
+            if (this.onSource) this.onSource({kind: "trim", node: text, output: trimmed, to: trimmed.text.length});
+          }
         }
       }
       var content = Fragment.from(this.content);
       if (!openEnd && this.match) content = content.append(this.match.fillBefore(Fragment.empty, true));
-      return this.type ? this.type.create(this.attrs, content, this.marks) : content;
+      var result = this.type ? this.type.create(this.attrs, content, this.marks) : content;
+      if (this.onSource) this.onSource({kind: "finish", node: result, children: this.content});
+      return result;
     }
   }, {
     key: "inlineContext",
@@ -2280,7 +2297,7 @@ var ParseContext = function () {
     var topNode = options.topNode,
       topContext;
     var topOptions = wsOptionsFor(null, options.preserveWhitespace, 0) | (isOpen ? OPT_OPEN_LEFT : 0);
-    if (topNode) topContext = new NodeContext(topNode.type, topNode.attrs, Mark.none, true, options.topMatch || topNode.type.contentMatch, topOptions);else if (isOpen) topContext = new NodeContext(null, null, Mark.none, true, null, topOptions);else topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions);
+    if (topNode) topContext = new NodeContext(topNode.type, topNode.attrs, Mark.none, true, options.topMatch || topNode.type.contentMatch, topOptions, options.onSource);else if (isOpen) topContext = new NodeContext(null, null, Mark.none, true, null, topOptions, options.onSource);else topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions, options.onSource);
     this.nodes = [topContext];
     this.find = options.findPositions;
     this.needsBlock = false;
@@ -2299,30 +2316,37 @@ var ParseContext = function () {
     key: "addTextNode",
     value: function addTextNode(dom, marks) {
       var value = dom.nodeValue;
+      var source = this.options.onSource ? {dom: dom, input: value, replacements: []} : undefined;
       var top = this.top,
         preserveWS = top.options & OPT_PRESERVE_WS_FULL ? "full" : this.localPreserveWS || (top.options & OPT_PRESERVE_WS) > 0;
       var schema = this.parser.schema;
       if (preserveWS === "full" || top.inlineContext(dom) || /[^ \t\r\n\u000c]/.test(value)) {
         if (!preserveWS) {
-          value = value.replace(/[ \t\r\n\u000c]+/g, " ");
+          value = replaceSourceText(value, /[ \t\r\n\u000c]+/g, " ", source);
           if (/^[ \t\r\n\u000c]/.test(value) && this.open == this.nodes.length - 1) {
             var nodeBefore = top.content[top.content.length - 1];
             var domNodeBefore = dom.previousSibling;
-            if (!nodeBefore || domNodeBefore && domNodeBefore.nodeName == 'BR' || nodeBefore.isText && /[ \t\r\n\u000c]$/.test(nodeBefore.text)) value = value.slice(1);
+            if (!nodeBefore || domNodeBefore && domNodeBefore.nodeName == 'BR' || nodeBefore.isText && /[ \t\r\n\u000c]$/.test(nodeBefore.text)) { if (source) source.replacements.push({from: 0, to: 1, text: ""}); value = value.slice(1); }
           }
         } else if (preserveWS === "full") {
-          value = value.replace(/\r\n?/g, "\n");
+          value = replaceSourceText(value, /\r\n?/g, "\n", source);
         } else if (schema.linebreakReplacement && /[\r\n]/.test(value) && this.top.findWrapping(schema.linebreakReplacement.create())) {
           var lines = value.split(/\r?\n|\r/);
+          var sourceOffset = 0;
           for (var i = 0; i < lines.length; i++) {
-            if (i) this.insertNode(schema.linebreakReplacement.create(), marks, true);
-            if (lines[i]) this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]));
+            if (i) {
+              var end = sourceOffset + (value[sourceOffset] === "\r" && value[sourceOffset + 1] === "\n" ? 2 : 1);
+              this.insertNode(schema.linebreakReplacement.create(), marks, true, source && Object.assign({}, source, {from: sourceOffset, to: end}));
+              sourceOffset = end;
+            }
+            if (lines[i]) this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]), source && Object.assign({}, source, {from: sourceOffset, to: sourceOffset + lines[i].length}));
+            sourceOffset += lines[i].length;
           }
           value = "";
         } else {
-          value = value.replace(/\r?\n|\r/g, " ");
+          value = replaceSourceText(value, /\r?\n|\r/g, " ", source);
         }
-        if (value) this.insertNode(schema.text(value), marks, !/\S/.test(value));
+        if (value) this.insertNode(schema.text(value), marks, !/\S/.test(value), source && Object.assign({}, source, {from: 0, to: value.length}));
         this.findInText(dom);
       } else {
         this.findInside(dom);
@@ -2440,8 +2464,9 @@ var ParseContext = function () {
         this.addElement(dom, marks, continueAfter);
       } else if (rule.getContent) {
         this.findInside(dom);
-        rule.getContent(dom, this.parser.schema).forEach(function (node) {
-          return _this5.insertNode(node, marks, false);
+        var generatedSource;
+        rule.getContent(dom, this.parser.schema, function (source) { generatedSource = source; }).forEach(function (node) {
+          return _this5.insertNode(node, marks, false, undefined, dom, generatedSource);
         });
       } else {
         var contentDOM = dom;
@@ -2486,7 +2511,7 @@ var ParseContext = function () {
     }
   }, {
     key: "insertNode",
-    value: function insertNode(node, marks, cautious) {
+    value: function insertNode(node, marks, cautious, source, generated, generatedSource) {
       if (node.isInline && this.needsBlock && !this.top.type) {
         var block = this.textblockFromContext();
         if (block) marks = this.enterInner(block, null, marks);
@@ -2509,7 +2534,10 @@ var ParseContext = function () {
         } finally {
           _iterator.f();
         }
-        top.content.push(node.mark(nodeMarks));
+        var output = node.mark(nodeMarks);
+        top.content.push(output);
+        if (source) this.options.onSource({kind: "text", node: output, source: source});
+        else if (generated && this.options.onSource) this.options.onSource({kind: "generated", node: output, dom: generated, source: generatedSource});
         return true;
       }
       return false;
@@ -2539,7 +2567,7 @@ var ParseContext = function () {
         }
         return true;
       });
-      this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options));
+      this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options, this.options.onSource));
       this.open++;
       return marks;
     }
diff --git a/node_modules/prosemirror-model/dist/index.d.cts b/node_modules/prosemirror-model/dist/index.d.cts
index b041bef..ffdbc70 100644
--- a/node_modules/prosemirror-model/dist/index.d.cts
+++ b/node_modules/prosemirror-model/dist/index.d.cts
@@ -458,7 +458,22 @@ These are the options recognized by the
 [`parse`](https://prosemirror.net/docs/ref/#model.DOMParser.parse) and
 [`parseSlice`](https://prosemirror.net/docs/ref/#model.DOMParser.parseSlice) methods.
 */
+interface DOMSourceText {
+  dom: Text
+  input: string
+  replacements: {from: number, to: number, text: string}[]
+  from: number
+  to: number
+}
+
+type DOMSourceEvent =
+  | {kind: "generated", node: Node, dom: HTMLElement, source?: {kind: string, value: unknown}}
+  | {kind: "text", node: Node, source: DOMSourceText}
+  | {kind: "trim", node: Node, output: Node, to: number}
+  | {kind: "finish", node: Node | Fragment, children: readonly Node[]}
+
 interface ParseOptions {
+    onSource?: (event: DOMSourceEvent) => void;
     /**
     By default, whitespace is collapsed as per HTML's rules. Pass
     `true` to preserve whitespace, but normalize newlines to
@@ -609,7 +624,7 @@ interface TagParseRule extends GenericParseRule {
     present, instead of parsing the node's child nodes, the result of
     this function is used.
     */
-    getContent?: (node: DOMNode, schema: Schema) => Fragment;
+    getContent?: (node: DOMNode, schema: Schema, onSource?: (source: {kind: string, value: unknown}) => void) => Fragment;
     /**
     Controls whether whitespace should be preserved when parsing the
     content inside the matched element. `false` means whitespace may
@@ -1680,4 +1695,4 @@ declare class Node {
     static fromJSON(schema: Schema, json: any): Node;
 }
 
-export { type AttributeSpec, type Attrs, ContentMatch, type DOMOutputSpec, DOMParser, DOMSerializer, Fragment, type GenericParseRule, Mark, type MarkSpec, MarkType, Node, NodeRange, type NodeSpec, NodeType, type ParseOptions, type ParseRule, ReplaceError, ResolvedPos, Schema, type SchemaSpec, Slice, type StyleParseRule, type TagParseRule };
+export { type DOMSourceText, type DOMSourceEvent, type AttributeSpec, type Attrs, ContentMatch, type DOMOutputSpec, DOMParser, DOMSerializer, Fragment, type GenericParseRule, Mark, type MarkSpec, MarkType, Node, NodeRange, type NodeSpec, NodeType, type ParseOptions, type ParseRule, ReplaceError, ResolvedPos, Schema, type SchemaSpec, Slice, type StyleParseRule, type TagParseRule };
diff --git a/node_modules/prosemirror-model/dist/index.d.ts b/node_modules/prosemirror-model/dist/index.d.ts
index b041bef..ffdbc70 100644
--- a/node_modules/prosemirror-model/dist/index.d.ts
+++ b/node_modules/prosemirror-model/dist/index.d.ts
@@ -458,7 +458,22 @@ These are the options recognized by the
 [`parse`](https://prosemirror.net/docs/ref/#model.DOMParser.parse) and
 [`parseSlice`](https://prosemirror.net/docs/ref/#model.DOMParser.parseSlice) methods.
 */
+interface DOMSourceText {
+  dom: Text
+  input: string
+  replacements: {from: number, to: number, text: string}[]
+  from: number
+  to: number
+}
+
+type DOMSourceEvent =
+  | {kind: "generated", node: Node, dom: HTMLElement, source?: {kind: string, value: unknown}}
+  | {kind: "text", node: Node, source: DOMSourceText}
+  | {kind: "trim", node: Node, output: Node, to: number}
+  | {kind: "finish", node: Node | Fragment, children: readonly Node[]}
+
 interface ParseOptions {
+    onSource?: (event: DOMSourceEvent) => void;
     /**
     By default, whitespace is collapsed as per HTML's rules. Pass
     `true` to preserve whitespace, but normalize newlines to
@@ -609,7 +624,7 @@ interface TagParseRule extends GenericParseRule {
     present, instead of parsing the node's child nodes, the result of
     this function is used.
     */
-    getContent?: (node: DOMNode, schema: Schema) => Fragment;
+    getContent?: (node: DOMNode, schema: Schema, onSource?: (source: {kind: string, value: unknown}) => void) => Fragment;
     /**
     Controls whether whitespace should be preserved when parsing the
     content inside the matched element. `false` means whitespace may
@@ -1680,4 +1695,4 @@ declare class Node {
     static fromJSON(schema: Schema, json: any): Node;
 }
 
-export { type AttributeSpec, type Attrs, ContentMatch, type DOMOutputSpec, DOMParser, DOMSerializer, Fragment, type GenericParseRule, Mark, type MarkSpec, MarkType, Node, NodeRange, type NodeSpec, NodeType, type ParseOptions, type ParseRule, ReplaceError, ResolvedPos, Schema, type SchemaSpec, Slice, type StyleParseRule, type TagParseRule };
+export { type DOMSourceText, type DOMSourceEvent, type AttributeSpec, type Attrs, ContentMatch, type DOMOutputSpec, DOMParser, DOMSerializer, Fragment, type GenericParseRule, Mark, type MarkSpec, MarkType, Node, NodeRange, type NodeSpec, NodeType, type ParseOptions, type ParseRule, ReplaceError, ResolvedPos, Schema, type SchemaSpec, Slice, type StyleParseRule, type TagParseRule };
diff --git a/node_modules/prosemirror-model/dist/index.js b/node_modules/prosemirror-model/dist/index.js
index 6b00598..127b5bc 100644
--- a/node_modules/prosemirror-model/dist/index.js
+++ b/node_modules/prosemirror-model/dist/index.js
@@ -2699,8 +2699,18 @@ function wsOptionsFor(type, preserveWhitespace, base) {
             (preserveWhitespace === "full" ? OPT_PRESERVE_WS_FULL : 0);
     return type && type.whitespace == "pre" ? OPT_PRESERVE_WS | OPT_PRESERVE_WS_FULL : base & ~OPT_OPEN_LEFT;
 }
+function replaceSourceText(value, pattern, replacement, source) {
+    if (!source) return value.replace(pattern, replacement);
+    let delta = 0;
+    return value.replace(pattern, (raw, offset) => {
+        if (raw !== replacement) source.replacements.push({from:offset+delta,to:offset+delta+raw.length,text:replacement});
+        delta += replacement.length - raw.length;
+        return replacement;
+    });
+}
 class NodeContext {
-    constructor(type, attrs, marks, solid, match, options) {
+    constructor(type, attrs, marks, solid, match, options, onSource) {
+        this.onSource = onSource;
         this.type = type;
         this.attrs = attrs;
         this.marks = marks;
@@ -2740,13 +2750,19 @@ class NodeContext {
                 if (last.text.length == m[0].length)
                     this.content.pop();
                 else
-                    this.content[this.content.length - 1] = text.withText(text.text.slice(0, text.text.length - m[0].length));
+                    {
+                    const trimmed = text.withText(text.text.slice(0, text.text.length - m[0].length));
+                    this.content[this.content.length - 1] = trimmed;
+                    this.onSource?.({kind:"trim",node:text,output:trimmed,to:trimmed.text.length});
+                }
             }
         }
         let content = Fragment.from(this.content);
         if (!openEnd && this.match)
             content = content.append(this.match.fillBefore(Fragment.empty, true));
-        return this.type ? this.type.create(this.attrs, content, this.marks) : content;
+        const result = this.type ? this.type.create(this.attrs, content, this.marks) : content;
+        this.onSource?.({kind:"finish",node:result,children:this.content});
+        return result;
     }
     inlineContext(node) {
         if (this.type)
@@ -2770,11 +2786,11 @@ class ParseContext {
         let topNode = options.topNode, topContext;
         let topOptions = wsOptionsFor(null, options.preserveWhitespace, 0) | (isOpen ? OPT_OPEN_LEFT : 0);
         if (topNode)
-            topContext = new NodeContext(topNode.type, topNode.attrs, Mark.none, true, options.topMatch || topNode.type.contentMatch, topOptions);
+            topContext = new NodeContext(topNode.type, topNode.attrs, Mark.none, true, options.topMatch || topNode.type.contentMatch, topOptions, options.onSource);
         else if (isOpen)
-            topContext = new NodeContext(null, null, Mark.none, true, null, topOptions);
+            topContext = new NodeContext(null, null, Mark.none, true, null, topOptions, options.onSource);
         else
-            topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions);
+            topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions, options.onSource);
         this.nodes = [topContext];
         this.find = options.findPositions;
         this.needsBlock = false;
@@ -2793,6 +2809,7 @@ class ParseContext {
     }
     addTextNode(dom, marks) {
         let value = dom.nodeValue;
+        const source = this.options.onSource ? {dom,input:value,replacements:[]} : undefined;
         let top = this.top, preserveWS = (top.options & OPT_PRESERVE_WS_FULL) ? "full"
             : this.localPreserveWS || (top.options & OPT_PRESERVE_WS) > 0;
         let { schema } = this.parser;
@@ -2800,7 +2817,7 @@ class ParseContext {
             top.inlineContext(dom) ||
             /[^ \t\r\n\u000c]/.test(value)) {
             if (!preserveWS) {
-                value = value.replace(/[ \t\r\n\u000c]+/g, " ");
+                value = replaceSourceText(value, /[ \t\r\n\u000c]+/g, " ", source);
                 // If this starts with whitespace, and there is no node before it, or
                 // a hard break, or a text node that ends with whitespace, strip the
                 // leading space.
@@ -2810,27 +2827,35 @@ class ParseContext {
                     if (!nodeBefore ||
                         (domNodeBefore && domNodeBefore.nodeName == 'BR') ||
                         (nodeBefore.isText && /[ \t\r\n\u000c]$/.test(nodeBefore.text)))
+                        {
+                        source?.replacements.push({from:0,to:1,text:""});
                         value = value.slice(1);
+                    }
                 }
             }
             else if (preserveWS === "full") {
-                value = value.replace(/\r\n?/g, "\n");
+                value = replaceSourceText(value, /\r\n?/g, "\n", source);
             }
             else if (schema.linebreakReplacement && /[\r\n]/.test(value) && this.top.findWrapping(schema.linebreakReplacement.create())) {
                 let lines = value.split(/\r?\n|\r/);
+                let sourceOffset = 0;
                 for (let i = 0; i < lines.length; i++) {
-                    if (i)
-                        this.insertNode(schema.linebreakReplacement.create(), marks, true);
+                    if (i) {
+                        const end = sourceOffset + (value[sourceOffset] === "\r" && value[sourceOffset+1] === "\n" ? 2 : 1);
+                        this.insertNode(schema.linebreakReplacement.create(), marks, true, source && {...source,from:sourceOffset,to:end});
+                        sourceOffset = end;
+                    }
                     if (lines[i])
-                        this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]));
+                        this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]), source && {...source,from:sourceOffset,to:sourceOffset+lines[i].length});
+                    sourceOffset += lines[i].length;
                 }
                 value = "";
             }
             else {
-                value = value.replace(/\r?\n|\r/g, " ");
+                value = replaceSourceText(value, /\r?\n|\r/g, " ", source);
             }
             if (value)
-                this.insertNode(schema.text(value), marks, !/\S/.test(value));
+                this.insertNode(schema.text(value), marks, !/\S/.test(value), source && {...source,from:0,to:value.length});
             this.findInText(dom);
         }
         else {
@@ -2959,7 +2984,8 @@ class ParseContext {
         }
         else if (rule.getContent) {
             this.findInside(dom);
-            rule.getContent(dom, this.parser.schema).forEach(node => this.insertNode(node, marks, false));
+            let generatedSource;
+            rule.getContent(dom, this.parser.schema, source => { generatedSource = source; }).forEach(node => this.insertNode(node, marks, false, undefined, dom, generatedSource));
         }
         else {
             let contentDOM = dom;
@@ -3015,7 +3041,7 @@ class ParseContext {
         return marks;
     }
     // Try to insert the given node, adjusting the context when needed.
-    insertNode(node, marks, cautious) {
+    insertNode(node, marks, cautious, source, generated, generatedSource) {
         if (node.isInline && this.needsBlock && !this.top.type) {
             let block = this.textblockFromContext();
             if (block)
@@ -3031,7 +3057,10 @@ class ParseContext {
             for (let m of innerMarks.concat(node.marks))
                 if (top.type ? top.type.allowsMarkType(m.type) : markMayApply(m.type, node.type))
                     nodeMarks = m.addToSet(nodeMarks);
-            top.content.push(node.mark(nodeMarks));
+            const output = node.mark(nodeMarks);
+            top.content.push(output);
+            if (source) this.options.onSource?.({kind:"text",node:output,source});
+            else if (generated) this.options.onSource?.({kind:"generated",node:output,dom:generated,source:generatedSource});
             return true;
         }
         return false;
@@ -3060,7 +3089,7 @@ class ParseContext {
             }
             return true;
         });
-        this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options));
+        this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options, this.options.onSource));
         this.open++;
         return marks;
     }
diff --git a/node_modules/prosemirror-model/src/from_dom.ts b/node_modules/prosemirror-model/src/from_dom.ts
index e6ae3d4..55407b8 100644
--- a/node_modules/prosemirror-model/src/from_dom.ts
+++ b/node_modules/prosemirror-model/src/from_dom.ts
@@ -10,7 +10,24 @@ import {DOMNode} from "./dom"
 /// These are the options recognized by the
 /// [`parse`](#model.DOMParser.parse) and
 /// [`parseSlice`](#model.DOMParser.parseSlice) methods.
+export interface DOMSourceText {
+  dom: Text
+  input: string
+  replacements: {from: number, to: number, text: string}[]
+  from: number
+  to: number
+}
+
+export type DOMSourceEvent =
+  | {kind: "generated", node: Node, dom: HTMLElement, source?: {kind: string, value: unknown}}
+  | {kind: "text", node: Node, source: DOMSourceText}
+  | {kind: "trim", node: Node, output: Node, to: number}
+  | {kind: "finish", node: Node | Fragment, children: readonly Node[]}
+
 export interface ParseOptions {
+  /// Observes actual text normalization and output identity without changing parsing.
+  onSource?: (event: DOMSourceEvent) => void
+
   /// By default, whitespace is collapsed as per HTML's rules. Pass
   /// `true` to preserve whitespace, but normalize newlines to
   /// spaces or, if available, [line break replacements](#model.NodeSpec.linebreakReplacement),
@@ -136,7 +153,7 @@ export interface TagParseRule extends GenericParseRule {
   /// Can be used to override the content of a matched node. When
   /// present, instead of parsing the node's child nodes, the result of
   /// this function is used.
-  getContent?: (node: DOMNode, schema: Schema) => Fragment
+  getContent?: (node: DOMNode, schema: Schema, onSource?: (source: {kind: string, value: unknown}) => void) => Fragment
 
   /// Controls whether whitespace should be preserved when parsing the
   /// content inside the matched element. `false` means whitespace may
@@ -337,6 +354,16 @@ function wsOptionsFor(type: NodeType | null, preserveWhitespace: boolean | "full
   return type && type.whitespace == "pre" ? OPT_PRESERVE_WS | OPT_PRESERVE_WS_FULL : base & ~OPT_OPEN_LEFT
 }
 
+function replaceSourceText(value: string, pattern: RegExp, replacement: string, source?: Pick<DOMSourceText, "replacements">) {
+  if (!source) return value.replace(pattern, replacement)
+  let delta = 0
+  return value.replace(pattern, (raw: string, offset: number) => {
+    if (raw !== replacement) source.replacements.push({from: offset + delta, to: offset + delta + raw.length, text: replacement})
+    delta += replacement.length - raw.length
+    return replacement
+  })
+}
+
 class NodeContext {
   match: ContentMatch | null
   content: Node[] = []
@@ -350,7 +377,8 @@ class NodeContext {
     readonly marks: readonly Mark[],
     readonly solid: boolean,
     match: ContentMatch | null,
-    public options: number
+    public options: number,
+    readonly onSource?: ParseOptions["onSource"]
   ) {
     this.match = match || (options & OPT_OPEN_LEFT ? null : type!.contentMatch)
   }
@@ -380,13 +408,19 @@ class NodeContext {
       if (last && last.isText && (m = /[ \t\r\n\u000c]+$/.exec(last.text!))) {
         let text = last as TextNode
         if (last.text!.length == m[0].length) this.content.pop()
-        else this.content[this.content.length - 1] = text.withText(text.text.slice(0, text.text.length - m[0].length))
+        else {
+          const trimmed = text.withText(text.text.slice(0, text.text.length - m[0].length))
+          this.content[this.content.length - 1] = trimmed
+          this.onSource?.({kind: "trim", node: text, output: trimmed, to: trimmed.text.length})
+        }
       }
     }
     let content = Fragment.from(this.content)
     if (!openEnd && this.match)
       content = content.append(this.match.fillBefore(Fragment.empty, true)!)
-    return this.type ? this.type.create(this.attrs, content, this.marks) : content
+    const result = this.type ? this.type.create(this.attrs, content, this.marks) : content
+    this.onSource?.({kind: "finish", node: result, children: this.content})
+    return result
   }
 
   inlineContext(node: DOMNode) {
@@ -414,11 +448,11 @@ class ParseContext {
     let topOptions = wsOptionsFor(null, options.preserveWhitespace, 0) | (isOpen ? OPT_OPEN_LEFT : 0)
     if (topNode)
       topContext = new NodeContext(topNode.type, topNode.attrs, Mark.none, true,
-                                   options.topMatch || topNode.type.contentMatch, topOptions)
+                                   options.topMatch || topNode.type.contentMatch, topOptions, options.onSource)
     else if (isOpen)
-      topContext = new NodeContext(null, null, Mark.none, true, null, topOptions)
+      topContext = new NodeContext(null, null, Mark.none, true, null, topOptions, options.onSource)
     else
-      topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions)
+      topContext = new NodeContext(parser.schema.topNodeType, null, Mark.none, true, null, topOptions, options.onSource)
     this.nodes = [topContext]
     this.find = options.findPositions
     this.needsBlock = false
@@ -438,6 +472,7 @@ class ParseContext {
 
   addTextNode(dom: Text, marks: readonly Mark[]) {
     let value = dom.nodeValue!
+    const source: Omit<DOMSourceText, "from" | "to"> | undefined = this.options.onSource ? {dom, input: value, replacements: []} : undefined
     let top = this.top, preserveWS = (top.options & OPT_PRESERVE_WS_FULL) ? "full"
       : this.localPreserveWS || (top.options & OPT_PRESERVE_WS) > 0
     let {schema} = this.parser
@@ -445,7 +480,7 @@ class ParseContext {
         top.inlineContext(dom) ||
         /[^ \t\r\n\u000c]/.test(value)) {
       if (!preserveWS) {
-        value = value.replace(/[ \t\r\n\u000c]+/g, " ")
+        value = replaceSourceText(value, /[ \t\r\n\u000c]+/g, " ", source)
         // If this starts with whitespace, and there is no node before it, or
         // a hard break, or a text node that ends with whitespace, strip the
         // leading space.
@@ -455,21 +490,30 @@ class ParseContext {
           if (!nodeBefore ||
               (domNodeBefore && domNodeBefore.nodeName == 'BR') ||
               (nodeBefore.isText && /[ \t\r\n\u000c]$/.test(nodeBefore.text!)))
+            {
+            source?.replacements.push({from: 0, to: 1, text: ""})
             value = value.slice(1)
+          }
         }
       } else if (preserveWS === "full") {
-        value = value.replace(/\r\n?/g, "\n")
+        value = replaceSourceText(value, /\r\n?/g, "\n", source)
       } else if (schema.linebreakReplacement && /[\r\n]/.test(value) && this.top.findWrapping(schema.linebreakReplacement.create())) {
         let lines = value.split(/\r?\n|\r/)
+        let sourceOffset = 0
         for (let i = 0; i < lines.length; i++) {
-          if (i) this.insertNode(schema.linebreakReplacement.create(), marks, true)
-          if (lines[i]) this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]))
+          if (i) {
+            const end = sourceOffset + (value[sourceOffset] === "\r" && value[sourceOffset + 1] === "\n" ? 2 : 1)
+            this.insertNode(schema.linebreakReplacement.create(), marks, true, source && {...source, from: sourceOffset, to: end})
+            sourceOffset = end
+          }
+          if (lines[i]) this.insertNode(schema.text(lines[i]), marks, !/\S/.test(lines[i]), source && {...source, from: sourceOffset, to: sourceOffset + lines[i].length})
+          sourceOffset += lines[i].length
         }
         value = ""
       } else {
-        value = value.replace(/\r?\n|\r/g, " ")
+        value = replaceSourceText(value, /\r?\n|\r/g, " ", source)
       }
-      if (value) this.insertNode(schema.text(value), marks, !/\S/.test(value))
+      if (value) this.insertNode(schema.text(value), marks, !/\S/.test(value), source && {...source, from: 0, to: value.length})
       this.findInText(dom)
     } else {
       this.findInside(dom)
@@ -585,7 +629,8 @@ class ParseContext {
       this.addElement(dom, marks, continueAfter)
     } else if (rule.getContent) {
       this.findInside(dom)
-      rule.getContent(dom, this.parser.schema).forEach(node => this.insertNode(node, marks, false))
+      let generatedSource: {kind: string, value: unknown} | undefined
+      rule.getContent(dom, this.parser.schema, source => { generatedSource = source }).forEach(node => this.insertNode(node, marks, false, undefined, dom, generatedSource))
     } else {
       let contentDOM = dom
       if (typeof rule.contentElement == "string") contentDOM = dom.querySelector(rule.contentElement)!
@@ -638,7 +683,7 @@ class ParseContext {
   }
 
   // Try to insert the given node, adjusting the context when needed.
-  insertNode(node: Node, marks: readonly Mark[], cautious: boolean) {
+  insertNode(node: Node, marks: readonly Mark[], cautious: boolean, source?: DOMSourceText, generated?: HTMLElement, generatedSource?: {kind: string, value: unknown}) {
     if (node.isInline && this.needsBlock && !this.top.type) {
       let block = this.textblockFromContext()
       if (block) marks = this.enterInner(block, null, marks)
@@ -652,7 +697,10 @@ class ParseContext {
       for (let m of innerMarks.concat(node.marks))
         if (top.type ? top.type.allowsMarkType(m.type) : markMayApply(m.type, node.type))
           nodeMarks = m.addToSet(nodeMarks)
-      top.content.push(node.mark(nodeMarks))
+      const output = node.mark(nodeMarks)
+      top.content.push(output)
+      if (source) this.options.onSource?.({kind: "text", node: output, source})
+      else if (generated) this.options.onSource?.({kind: "generated", node: output, dom: generated, source: generatedSource})
       return true
     }
     return false
@@ -682,7 +730,7 @@ class ParseContext {
       }
       return true
     })
-    this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options))
+    this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, options, this.options.onSource))
     this.open++
     return marks
   }
diff --git a/node_modules/prosemirror-model/src/index.ts b/node_modules/prosemirror-model/src/index.ts
index e131cae..494484e 100644
--- a/node_modules/prosemirror-model/src/index.ts
+++ b/node_modules/prosemirror-model/src/index.ts
@@ -7,5 +7,5 @@ export {Mark} from "./mark"
 export {Schema, NodeType, Attrs, MarkType, NodeSpec, MarkSpec, AttributeSpec, SchemaSpec} from "./schema"
 export {ContentMatch} from "./content"
 
-export {DOMParser, GenericParseRule, TagParseRule, StyleParseRule, ParseRule, ParseOptions} from "./from_dom"
+export {DOMParser, GenericParseRule, TagParseRule, StyleParseRule, ParseRule, ParseOptions, DOMSourceText, DOMSourceEvent} from "./from_dom"
 export {DOMSerializer, DOMOutputSpec} from "./to_dom"
